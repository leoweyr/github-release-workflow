name: Prepare Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write
  pull-requests: write

jobs:
  call-prepare:
    uses: leoweyr/github-release-workflow/src/reusable-prepare-release.yml@master
    secrets: inherit
